{
  "name": "suivi-hotel-r2",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "check": "tsc --noEmit",
    "test": "tsc -p tsconfig.test.json && node --test .test-dist/tests/*.test.js tests/auth-ui.test.mjs",
    "test:database": "node --test tests/database.test.mjs"
  },
  "devDependencies": {
    "@electric-sql/pglite": "0.5.8",
    "@types/node": "24.0.0",
    "dxf-parser": "^1.1.2",
    "fake-indexeddb": "^6.2.5",
    "typescript": "^5.9.3",
    "vite": "8.3.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.117.1"
  }
}
